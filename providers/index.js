const cordProvider = require('./cord-provider')
const laptopProvider = require('./laptop-provider')
const socketProvider = require('./socket-provider')

module.exports = {
    cordProvider,
    laptopProvider,
    socketProvider
}
